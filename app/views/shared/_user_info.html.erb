<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 52 %>
</a>
<h1>
  <%= current_user.name %> <%= link_to "view profile", current_user ,class:"btn"%>
</h1>
<span>
  <%= pluralize(current_user.microposts.count, "micropost") %>
</span>